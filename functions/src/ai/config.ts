/**
 * AI config — GERİYE UYUMLU YÖNLENDİRME.
 * Tüm ayarlanabilir değerler artık merkezi ../config.ts'te toplanmıştır
 * (hotfix). Bu dosya mevcut import yollarını kırmamak için re-export eder.
 */
export {
  OPENAI_MODEL,
  MAX_OUTPUT_TOKENS,
  INITIAL_FREE_CREDITS,
  DAILY_SPEND_LIMIT_USD,
} from "../config.js";
