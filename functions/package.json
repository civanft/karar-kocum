{
  "name": "karar-veriyorum-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "lint": "eslint src",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:rules": "cd .. && npx --yes firebase-tools@15.22.4 emulators:exec --only firestore --project demo-karar \"cd functions && npx vitest run --config vitest.rules.config.ts\"",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth",
    "deploy": "firebase deploy --only functions",
    "test:privacy": "cd .. && npx --yes firebase-tools@15.22.4 emulators:exec --only firestore,auth --project demo-karar \"cd functions && npx vitest run --config vitest.privacy.config.ts\"",
    "test:emulator": "cd .. && npx --yes firebase-tools@15.22.4 emulators:exec --only firestore,auth --project demo-karar \"cd functions && npx vitest run --config vitest.emulator.config.ts\""
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.3.2",
    "openai": "^6.49.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^4.1.11",
    "eslint": "^9.39.5",
    "js-tiktoken": "^1.0.21",
    "typescript": "^5.5.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.11"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
