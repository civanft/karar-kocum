# Optional LOCAL dev smoke configuration. Never commit a populated .env.
# node --env-file=.env scripts/smoke/live_smoke_test.mjs
# Smoke writes to the DEV project. Do not run against production implicitly.
FIREBASE_WEB_API_KEY=
# OPENAI_API_KEY is deliberately absent: production uses Secret Manager;
# local Firebase emulators use functions/.secret.local (see SECURITY.md).
