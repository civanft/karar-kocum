# --- Flutter / Dart ---
.dart_tool/
.packages
.pub-cache/
.pub/
build/
coverage/
doc/api/
.flutter-plugins
.flutter-plugins-dependencies
migrate_working_dir/

# Üretilen kod — CI'da build_runner ile yeniden üretilir
*.g.dart
*.freezed.dart

# --- Firebase ---
# firebase_options.dart COMMIT EDİLİR (istemci kimlikleri gizli değildir);
# repo'daki placeholder, 'flutterfire configure' ile üzerine yazılır.
# Platform configleri public istemci kimlikleri taşır; repoda tek kaynak olarak
# lib/firebase_options.dart tutulur. Başka klasöre kopyaları da izlenmez.
**/google-services.json
**/GoogleService-Info.plist
# İSTİSNA: production istemci yapılandırmaları izlenir. Bunlar public
# kimliklerdir (yayınlanan her binary'de bulunur) ve release build'in
# hangi projeye bağlandığı Git'ten görülebilmelidir. Genel kural
# gevşetilmez — yalnız bu iki KESİN yol istisnadır.
!android/app/src/release/google-services.json
!ios/Runner/Firebase/Production/GoogleService-Info.plist
# .firebaserc commit edilir (yalnız proje takma adları)
firebase-debug.log
.firebase/

# --- Cloud Functions ---
functions/node_modules/
functions/lib/
functions/.env*
functions/*.log

# --- İmzalama ve gizli anahtarlar — ASLA commit edilmez ---
*.keystore
*.jks
key.properties
*.p8
*.p12
*.pfx
*.pem
*.key
*.cer
*.crt
*.der
*.pkcs8
*.mobileprovision
*.provisionprofile
*serviceAccount*.json
*service-account*.json
*service_account*.json
*-firebase-adminsdk-*.json
application_default_credentials.json
credentials.json
.runtimeconfig.json
.npmrc
.netrc
.env
.env.*
!/.env.example
# Firebase emulator secret overrides are not ordinary .env files.
.secret.local
.secret.*
!/functions/.secret.local.example
!/functions/.env.example
.secrets/
.gcloud/
id_rsa
id_ed25519
id_ecdsa
__pycache__/
*.py[cod]

# --- IDE ---
.idea/
.vscode/
*.iml
*.ipr
*.iws

# --- İşletim sistemi ---
.DS_Store
Thumbs.db

# --- Diğer ---
*.log
*.swp
.history
.atom/
.claude/launch.json
scripts/smoke/node_modules/
# vitest çalışma süresi önbelleği (üretilmiş çıktı)
.vite/
